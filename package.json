{
  "name": "guess-the-gibberish",
  "version": "1.0.0",
  "description": "A multiplayer game where players guess gibberish phrases",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "build": "echo 'No build step required for this project'",
    "heroku-postbuild": "echo 'No build step required for this project'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
